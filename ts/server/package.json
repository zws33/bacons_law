{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@baconslaw/server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "@baconslaw/tsconfig": "workspace:*",
    "@types/node": "^24.0.0",
    "typescript": "^7.0.0"
  }
}
