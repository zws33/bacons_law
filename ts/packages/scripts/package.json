{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@baconslaw/scripts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo"
  },
  "devDependencies": {
    "@baconslaw/tsconfig": "workspace:*",
    "@types/node": "^24.0.0",
    "typescript": "^7.0.0"
  }
}
