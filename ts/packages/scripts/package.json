{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@baconslaw/scripts",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": "./dist/index.js"
  },
  "scripts": {
    "dev": "tsc --build --watch",
    "build": "tsc --build",
    "typecheck": "tsc --build",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest --passWithNoTests"
  },
  "devDependencies": {
    "@baconslaw/tsconfig": "workspace:*",
    "@types/node": "^24.0.0",
    "typescript": "^7.0.0",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "csv-stringify": "^6.8.3",
    "@baconslaw/lib": "workspace:*"
  }
}
