# Root is stack-agnostic — keep this generic.
# Per-project ignores (Gradle, Python, etc.) live in each top-level project dir,
# e.g. kotlin/.gitignore.

# IDE
.idea/
*.iml

# OS
.DS_Store

# Local env / secrets
.env
worktrees/
