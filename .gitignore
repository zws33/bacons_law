# Root is stack-agnostic — keep this generic.
# Per-project ignores (Python, Node, etc.) live in each top-level project dir,
# e.g. etl/.gitignore.

# IDE
.idea/
*.iml

# OS
.DS_Store

# Local env / secrets
.env
worktrees/
